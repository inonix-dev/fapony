.DS_Store
node_modules/
# Bun-only repo — bun.lock is the committed lockfile; pnpm regenerates this via `pnpm pack`
pnpm-lock.yaml
*.db*
wt-*/
cl-*/
*-TEMP.md

# Personal machine config — see fapony.config.example.json
.fapony/
fapony.config.json
scripts/*
!scripts/smoke-publish.sh
!scripts/test-one.ts
!scripts/codemod-bun-test.ts

# Added by code-review-graph
.code-review-graph/

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE / tool configs (local paths)
.claude/
.mcp.json
.opencode/
.vscode/
.cursor/
.cursorrules
.windsurfrules
opencode.jsonc
.fapony/plan/PLAN-bun-test-migration.md

# Agent rules — local to this machine (public repo; README is the public doc)
CLAUDE.md
AGENTS.md
